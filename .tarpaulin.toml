[global]
exclude-files = [
    "src/main.rs",
    "src/docs.rs"
]
target-dir = "target/tarpaulin"
run-types = ["Tests"]
timeout = "1m"

ignore-tests = false
skip-clean = true
verbose = true
line = true

jobs = 4
fail-under = 90

[report]
output-dir = "coverage"
out = ["Xml"]